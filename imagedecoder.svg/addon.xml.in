<?xml version="1.0" encoding="UTF-8"?>
<addon
  id="imagedecoder.svg"
  version="0.0.1"
  name="SVG image decoder"
  provider-name="cinemaONE">
  <requires>@ADDON_DEPENDS@</requires>
  <extension
    point="kodi.imagedecoder"
    library_@PLATFORM@="@LIBRARY_FILENAME@">
    <support>
      <mimetype name="image/svg+xml">
        <extension>.svg</extension>
        <description>Scalable Vector Graphics</description>
      </mimetype>
      <!-- Kodi's own ImageFactory::CreateLoader() (guilib/imagefactory.cpp) builds the
           mimetype it looks up as "image/"+CURL::GetFileType() whenever the file extension
           is non-empty - which it is for any plain <texture>foo.svg</texture> skin reference -
           bypassing the correct CMime::GetMimeType() -> image/svg+xml lookup entirely. Declare
           this non-standard "image/svg" too so that actual skin texture loading matches. -->
      <mimetype name="image/svg">
        <extension>.svg</extension>
        <description>Scalable Vector Graphics</description>
      </mimetype>
    </support>
  </extension>
  <extension point="xbmc.addon.metadata">
    <platform>@PLATFORM@</platform>
    <license>GPL-2.0-or-later</license>
    <assets>
      <icon>resources/icon.png</icon>
    </assets>
    <summary lang="en_GB">SVG file format decoder</summary>
    <description lang="en_GB">Scalable Vector Graphics (SVG) is a vector image format, rendered
sharp at any requested resolution instead of a single fixed pixel size.[CR][CR]Rendering is
provided by lunasvg (https://github.com/sammycage/lunasvg), MIT licensed.</description>
  </extension>
</addon>
